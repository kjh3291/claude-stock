# 팀 공유사항 (변경 기록)

> 팀원용 변경·결정 로그. **위가 최신.** 무엇을·왜 바꿨는지, 팀원이 알아야 할 로컬 설정 변경까지 남깁니다.

## 2026-09-17

**요약:** 목표 의사결정 루프(`team.py`)를 매일 자동실행에 올리고, 팀장 확신도 낮은 후보는 매수 보류하도록 개선.

- **launchd 자동실행을 `live.py` → `team.py`로 교체** (로컬 설정, 깃 밖)
  - `~/Library/LaunchAgents/com.claudestock.live.plist`의 실행 스크립트를 team.py로 변경, 로그도 `team.log`로 분리.
  - 이유: 목표 루프(스크리너→팀장 분석→포트폴리오)가 수동 실행만 돼서 포워드 검증 데이터가 목표 구조로 안 쌓였음. 이제 평일 18:00 자동 수집.
  - 옛 `live.py`(종목별 개별)는 코드로 남겨둠 — 실험/비교용, 자동에서만 뺐음.
- **`team.py`: 팀장 확신도(conviction) < 0.5 후보는 매수 보류** (`keep_candidate()`)
  - 이유: 기존엔 팀장이 "확신 부족/촉매 불명확"이라 해도 veto만 아니면 규칙이 그냥 매수했음. hold 성격 후보를 걸러냄.
  - **안전원칙 유지:** 팀장 부재/무의견(conviction=None)이면 그대로 규칙 매수 통과 → Claude 없어도 시스템은 규칙만으로 동작.
  - 점검: `python team.py check` (네트워크 없이 필터 로직 assert).
- **결정: 미국주식 매도세 정확화는 보류.** paper_wallet이 미국에도 국내 거래세(0.18%)를 매기는 건 **의도적 비용 과대추정**(보수적). "안 잃기" 검증엔 안전한 방향이라 유지. 시장별 분기는 실거래(토스 연동) 단계에서.

**다음 스프린터(작업 중):** 규칙 개선 실험 — 파이오니어 매수조건 완화, 스프린터 익손절 튜닝. 반드시 `tune.py`(훈련/검증 분리) → `validate.py`(여러 국면) 통과분만 채택. 전체 로드맵은 팀 내 플랜 참고.
