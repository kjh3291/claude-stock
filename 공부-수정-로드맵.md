# claude-stock 공부·수정 로드맵

- 작성일: 2026-09-19
- 근거: 코드 점검, 기획서 대비 구현 비교, 학습 자료 조사(세 갈래 병렬 조사). 이 조사에서 파일은 수정하지 않음.
- 표시: `(추정)` 은 확인하지 못한 내용이다.

---

## 결론

**기록 → 체결·비용 → 성적 해석 → 검증 → 규칙 → 실거래** 순서로 진행한다. 각 단계는 그 단계에 필요한 주식 지식을 먼저 익힌 뒤 고친다. 앞 단계가 틀려 있으면 뒤 단계에서 나온 숫자를 믿을 수 없기 때문이다. 다만 주식 지식이 필요 없는 버그 5개는 공부와 함께 바로 고친다(3장).

---

## 1. 지금 성적표가 실제보다 좋게 나오는 이유는 네 가지다

### 가) 체결 — 판단한 그 가격에 바로 체결한다

- 당일 종가로 신호를 내고 같은 종가에 체결한다(`main.py:105-146`, `live.py`, `team.py`).
  - 실제로는 18:00에 판단하므로 빨라도 다음 날 시가에 체결된다.
- 매수 수량 `max(1, int(cash*cap/price))` 때문에 예산이 모자라도 무조건 1주를 산다(`main.py:135`, `live.py:112`, `team.py:114`).
  - 그래서 종목상한이 국내 종목에는 사실상 적용되지 않는다.
- 원화 손익과 달러 손익을 그냥 더해 파라미터 순위를 매긴다(`tune.py`).

### 나) 검증 — "통과"가 과최적화를 거르지 못한다

- `tune.py`가 검증 구간 성적으로 정렬해서 값을 고른다. 검증 구간을 이미 선택에 썼다.
- `validate.py`는 값을 고를 때 쓴 두 시대로 다시 검사한다. 순환 검증이다.
- 통과 기준이 너무 느슨하다. 32개 중 16등도 통과한다.
- 벤치마크(buy&hold, 지수)가 없다.
- 튜닝 종목이 결과를 알고 고른 승자들이다. 스크리너는 현재 S&P500 구성을 쓴다(생존편향).

### 다) 운영 — 무인 실행이 실패해도 알 수 없다

- `team.py`에 최상위 예외 처리가 없다. 스크리너가 실패하면 손절과 서킷 브레이커도 돌지 않는다.
- 팀장(Claude)이 실패하거나 응답이 빠지면 조용히 규칙대로 매수한다. conviction이 문자열로 오면 TypeError로 그날 실행이 멈춘다.
- launchd PATH에 `~/.local/bin`이 없으면 팀장이 한 번도 호출되지 않았을 수 있다 `(추정)`.
- 실행 여부를 달력 날짜로 판단한다(`team.py:75`). 미국 휴장일 다음 날 같은 봉을 다시 처리한다.
- 마라토너 매도 신호는 추세가 꺾인 그날에만 나온다(`marathoner_brain.py:51`). 그날 실행을 놓치면 -15% 손절까지 간다.
- 일별 평가금액을 저장하지 않는다. 포워드 검증을 끝내도 MDD나 지수 대비 성적을 낼 수 없다.

### 라) 정합 — 문서·코드·프롬프트의 숫자가 다르다

| 항목 | 기획서 | 코드 | 프롬프트 |
|---|---|---|---|
| 스프린터 익절/손절 | +3% / -2% | +5% / -1.5% | +5% / -1.5% |
| 파이오니어 RSI 기준 | 30 이하 | 35 (주석은 30) | 40 (코칭 로그는 35) |
| 스프린터 배분 | 30% | 30% | "실거래 제외" |
| LLM 역할 | 설명만 | 매수 거름망(veto·확신도) | — |

---

## 2. 우선순위별 로드맵: 먼저 알아야 할 것 → 고칠 것

| 순위 | 먼저 알아야 할 주식 지식 | 그 지식으로 고칠 것 | 공부 시간 | 완료 기준 |
|---|---|---|---|---|
| **1. 기록** | 평가금액, 수익률, MDD, 벤치마크 | `team.py`에 일별 평가금액 CSV, SPY 대비 수익률, 팀장 응답 로그 추가 | 3~4시간 | 매일 한 줄씩 쌓이고 MDD·지수 대비가 계산됨 |
| **2. 체결·비용** | 장 시간, 종가와 시가, 1주 단위, 수수료·거래세·슬리피지, 환율 | 다음 날 시가 체결, `max(1, …)` 제거, 통화 분리와 수익률 집계, 거래세 0.20% 반영 | 4~6시간 | 수정 전후 성적 비교표가 나옴 |
| **3. 성적 해석** | 승률과 기대값의 차이, Sharpe, buy&hold | 승률에 비용 반영, 기대값·Sharpe·보유 비중 추가 | 4~6시간 | 성적표에 "그냥 들고 있었으면" 열이 생김 |
| **4. 검증** | 과최적화, 생존편향, 표본 밖 검증, walk-forward | holdout 구간 분리, 무작위 종목군으로 튜닝, 통과 기준 강화 | 1주 | 튜닝에 안 쓴 구간의 성적이 따로 보고됨 |
| **5. 규칙** | 이동평균, 골든크로스, RSI, 추세추종과 역추세 | 마라토너 매도를 상태 기반으로, 파이오니어 조건 재검토, 스프린터 배분 결정, 숫자 통일 | 3~4시간 + 실험 | 4단계 검증을 통과한 값만 채택 |
| **6. 실거래** | 주문 유형, 가격제한폭 ±30%, 부분체결, 해외주식 양도세, 브로커 API | 추천·승인 모드, 주문 한도, 알림, 긴급 전체 정지, 잔고 대사, 모의투자 연습 | 각 1~2주 | 실거래 전환 수치 기준 통과 |

**순서를 이렇게 정한 이유**

- **기록이 1순위인 이유:** 포워드 검증은 2026-09-17부터 돌고 있다. 기록을 늦추는 만큼 데이터를 버린다.
- **체결·비용이 2순위인 이유:** 지금 성적 숫자가 부풀려져 있어서, 이걸 고치지 않으면 3~5단계의 판단이 모두 틀린 숫자를 근거로 삼게 된다.
- **규칙이 5순위인 이유:** 공유사항의 다음 작업(파이오니어 완화, 스프린터 튜닝)은 2~4단계를 끝낸 뒤에야 의미가 있다.

---

## 3. 주식 지식 없이 지금 고칠 것 (병행)

| 고칠 것 | 위치 | 그대로 두면 |
|---|---|---|
| 최상위 예외 처리와 오류 로그 | `team.py` | 실패한 날 손절과 서킷 브레이커가 돌지 않고 아무도 모름 |
| conviction 숫자 변환, verdict 정규화, hold를 매수에서 제외 | `analyst.py`, `team.py:39` | 문자열이 오면 그날 실행이 멈춤 |
| launchd에서 `claude` 경로 확인 | plist, `analyst.py` | 팀장이 호출되지 않았을 수 있음 `(추정)` |
| 실행 여부를 데이터 날짜로 판단 | `team.py:75` | 휴장일 다음 날 같은 봉을 다시 처리 |
| 상태 파일 원자적 저장과 백업 | `team.py`, `live.py` | 파일이 깨지면 이후 매일 실패 |

---

## 4. 공부할 것 상세

### 가) 금융·매매 기초 — 먼저

| 주제 | 이 저장소에서 왜 필요한가 | 시간 |
|---|---|---|
| 이동평균·골든크로스·RSI | 세 트레이너 규칙이 전부 이것 (`*_brain.py`) | 3~4시간 |
| 수수료·거래세·슬리피지 | `paper_wallet.py:30-32`. 스프린터는 비용 때문에 전 종목 적자 | 2시간 |
| MDD, 승률과 기대값, Sharpe | `main.py`의 승률이 비용을 무시함. 기대값·Sharpe 계산이 없음 | 4~6시간 |
| 장 시간·가격제한폭·호가 | 실거래 전 필요 | 2시간 |

**확인된 사실**

- KRX 정규장은 09:00~15:30이다.
- 가격제한폭은 전일 종가의 ±30%다.
- 넥스트레이드(NXT) 거래 시간은 다음과 같다.
  - 프리마켓: 08:00~08:50
  - 애프터마켓: 15:30~20:00
- 2026년부터 코스피·코스닥 증권거래세는 0.20%다. 코드는 0.18%다(`paper_wallet.py:31`). 조사 출처가 있으나 직접 재확인이 필요하다.

**자료**

- [Investopedia – Sharpe Ratio](https://www.investopedia.com/terms/s/sharperatio.asp)
- [ta 라이브러리 문서](https://technical-analysis-library-in-python.readthedocs.io/)
- [넥스트레이드 거래제도](https://nextrade.co.kr/transactionSys/content.do)

### 나) 백테스트 방법론 — 먼저

| 주제 | 왜 필요한가 | 시간 |
|---|---|---|
| 미래 참조(look-ahead) | 1장 가) — 같은 종가 체결 | 2시간 |
| 생존편향 | 1장 나) — 현재 S&P500 구성 사용 | 2시간 |
| 과최적화·표본 밖 검증·다중검정 | `tune.py`의 grid, `validate.py` | 1주 |
| walk-forward | 60/40 분할 한 번을 반복 검증으로 개선 | 4~6시간 |

**자료**

- Bailey 외, [The Probability of Backtest Overfitting](https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2326253)
- Bailey & López de Prado, [The Deflated Sharpe Ratio](https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2460551)
- Ernest Chan, *Quantitative Trading* (편향 장)
- Robert Pardo, *The Evaluation and Optimization of Trading Strategies* (walk-forward)

### 다) 도구

| 도구 | 우선 | 볼 것 | 자료 |
|---|---|---|---|
| pandas | 먼저 | `rolling`·`shift`·`iloc`. `shift(1)`이 미래 참조를 막는 핵심 | [10 minutes to pandas](https://pandas.pydata.org/docs/user_guide/10min.html) |
| FinanceDataReader | 먼저 | 데이터 출처 접두어(`KRX:`, `YAHOO:`), 수정주가 여부는 직접 확인. `StockListing('KRX')` 오류 이력 | [GitHub](https://github.com/FinanceData/FinanceDataReader) |
| pytest | 다음 | 각 파일 `__main__` assert를 `tests/`로 이전 | [시작하기](https://docs.pytest.org/en/stable/getting-started.html) |
| backtesting.py | 다음 | 설치 안내에만 있고 쓰지 않음. 직접 짠 엔진 교차검증용. `trade_on_close`·`commission` | [API 문서](https://kernc.github.io/backtesting.py/doc/backtesting/backtesting.html) |
| launchd | 나중 | 휴장일, 잠자기 중 놓친 실행, PATH, 로그 | [launchd.info](https://www.launchd.info/) |

### 라) LLM을 루프에 넣는 법 — 다음

- `claude -p --output-format json --json-schema '<schema>'`로 받으면 `analyst.py`에서 정규식으로 JSON을 뽑는 부분을 대체할 수 있다. 결과에 `total_cost_usd`도 들어 있다.
- `--bare`는 API 키가 필요하다. 구독을 유지하려면 쓰지 않는다.
- LLM 판단은 조언용으로만 둔다.
  - 같은 입력에도 출력이 달라져 재현이 어렵다.
  - 학습 이후의 시장을 모른다.
  - 과거를 백테스트할 때 모델이 이미 그 뒤 결과를 알고 있을 수 있다.
- 자료: [Claude Code 헤드리스 모드](https://code.claude.com/docs/en/headless)

### 마) 실거래로 가는 길 — 나중

| 증권사 API | 확인된 것 | 자료 |
|---|---|---|
| 토스증권 Open API | 2026-08-13 전 고객 대상 정식 출시. 모의투자 서버는 확인 못 함 | [개발자센터](https://developers.tossinvest.com/) |
| 한국투자증권 KIS | REST·WebSocket, 모의투자 있음. 초당 20건 한도 `(추정)` | [포털](https://apiportal.koreainvestment.com/intro), [샘플](https://github.com/koreainvestment/open-trading-api) |
| 키움 REST | 모의투자 도메인이 따로 있음 | [포털](https://openapi.kiwoom.com/) |

- 같이 공부할 것
  - 주문 유형(지정가·시장가), 정정·취소, 부분체결
  - 호출 한도 초과 시 백오프
  - 토큰 갱신
  - 비밀값 관리(`.env`와 Keychain)
- `.gitignore`에 `.env` 추가 필요.
- 초보는 모의투자가 확실한 KIS·키움으로 주문 흐름을 먼저 연습한다.

---

## 5. 다음 할 일

1. **공부:** 1~2순위 범위(평가금액·MDD·벤치마크, 체결 시점, 거래 비용). 하루 분량이다.
2. **수정:** 1순위 기록 보강과 3장 버그 수정. 둘 다 `team.py`에 모여 있다.
3. **팀 결정 요청:** 두 가지를 정해야 한다.
   - 실거래 전환 수치 기준(예: MDD ≤ X%, 지수 대비 ≥ 0, 거래 N회 이상)
   - 스프린터 30% 배분 유지 여부
4. **후순위:** Slack `stock-build` 채널 정리(주제·설명·고정 메시지, GitHub 앱 연결 여부).

## 미해결

| 항목 | 확인 방법 |
|---|---|
| launchd 실행 시 `claude`를 찾는지 | `team.log`에서 팀장 응답 여부 확인 |
| FDR 가격이 수정주가인지 | 삼성전자 2018년 액면분할 전후 가격 확인 |
| 증권거래세 0.20% | 국세청·KRX 공지로 재확인 |
| 토스 Open API 모의투자·호출 한도 | 개발자센터 문서 확인 |
